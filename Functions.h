#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int isSavedName(char*);
int checkInst(char *);
int checkOpSrc(int,int);
int checkOpDst(int,int);
int checkValidNum(char*);
